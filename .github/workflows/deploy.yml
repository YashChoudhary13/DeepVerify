name: Deploy to Droplet

on:
  push:
    branches: [main]
    # Backend-only deploy: frontend (Vercel) and docs must not trigger a Droplet rebuild.
    paths-ignore:
      - 'frontend/**'
      - '**.md'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://deepverify.site

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DO_HOST }}
          username: root
          key: ${{ secrets.DO_SSH_KEY }}
          command_timeout: 20m
          script: |
            cd /apps/deepverify
            git pull origin main
            docker compose -f docker-compose.prod.yml up -d --build
            docker image prune -f
